future works:
    can increase the number of 'tasks'


# ## Choose the task
# tasks = {
#     "Email" : 'Improve English in following email: \n'
# }
# list_of_tasks = [k for k in tasks.keys()]
# current_task = input("The task for the chatbot is >>> ")
# while current_task not in list_of_tasks:
#     current_task = input(f"Choose a task from: {list_of_tasks} >>> ")
# instruction = tasks[current_task]